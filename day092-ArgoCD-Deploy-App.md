# ArgoCD Deploy App

Steps:
1. Create Git repo with YAML
2. Open ArgoCD UI
3. Create application
4. Sync application

GitOps Flow:
Git → ArgoCD → Kubernetes

Commands:
kubectl get pods
kubectl get svc
