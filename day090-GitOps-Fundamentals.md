# GitOps

Git is source of truth.

Flow:
Developer → Git → GitOps tool → Kubernetes

Benefits:
- Automatic deployment
- Self healing
- Git history
- Easy rollback

Tools:
- ArgoCD
- Flux

GitOps = Pull model
CI/CD = Push model
